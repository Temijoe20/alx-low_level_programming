#include "main.h"
/**
 * mul - this funcion multiplies two numbers
 * @a: first number
 * @b: second number
 * this code should run, the previous didnt
 * Return: result
 */
int mul(int a, int b)
{
return (a * b);
}
